// `dimension` module nested inside the `dimension` directory (see
// catalog/mod.rs for the rationale behind this deliberate `module_inception`).
#[allow(clippy::module_inception)]
pub mod dimension;
pub mod hierarchy;
//pub mod dictionary;
